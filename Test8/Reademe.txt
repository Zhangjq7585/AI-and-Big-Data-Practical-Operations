test8:解决SVM多分类过拟合的方法
	 在多分类任务（如鸢尾花品种分类，3个类别）中，使用SVM模型时，发现模型在测试集上存在过拟合现象。
	请说明至少3种解决过拟合的方法，并写出对应的代码调整方式。
	
test8解答
	1.增加正则化强度。SVM中通过C参数控制正则化（C越小，正则化越强，避免过拟合）；
	2.使用核函数正则化。对RBF核，通过gamma参数控制核函数复杂度(gamma越小，核函数越简单，
	减少过拟合)；
	3.数据增强。增加训练样本数量（如对图像数据旋转、裁剪、对结构化数据添加轻微噪声）；
	4.特征选择。删除冗余或噪声特征、降低模型输入维度。
